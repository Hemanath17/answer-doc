[phases.setup]
nixPkgs = ["python312", "gcc", "libffi", "openssl", "zlib", "poppler_utils"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r backend/requirements.txt"]

[start]
cmd = "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
